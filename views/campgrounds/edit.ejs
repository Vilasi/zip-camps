<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Edit Camp</title>
    <meta name="description" content="" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="" />
  </head>
  <body>
    <h1>Edit campground</h1>
    <form action="/campgrounds/<%= camp._id %>?_method=PATCH" method="post">
      <!--* Title -->
      <div>
        <!--* Setting value to camp.title will pre-populate our input with the current title -->
        <label for="title">Title</label>
        <input
          type="text"
          name="campground[title]"
          id="title"
          value="<%= camp.title %>"
        />
      </div>

      <div>
        <label for="location">Location</label>
        <input
          type="text"
          name="campground[location]"
          id="location"
          value="<%= camp.location %>"
        />
      </div>

      <button type="submit">Update</button>
    </form>

    <a href="/campgrounds/<%= camp._id %>">Back to Campground</a>

    <script src="" async defer></script>
  </body>
</html>
